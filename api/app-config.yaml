auth:
  oidc_client_id: "{{.OIDC_CLIENT_ID}}"
  oidc_issuer_url: "{{.OIDC_ISSUER_URL}}"
api:
  port: 3001
  log_level: info
database:
  driver: postgres
  data_source_name: host={{.POSTGRES_HOST}} user={{.POSTGRES_USER}} password={{.POSTGRES_PASSWORD}} port=5432
  log_level: info
